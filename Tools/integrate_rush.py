from pathlib import Path
root=Path(__file__).resolve().parents[1]
def edit(path,old,new):
 p=root/path;s=p.read_text(encoding='utf-8-sig');assert old in s,(path,old[:90]);p.write_text(s.replace(old,new),encoding='utf-8')
edit('Assets/Scripts/Game.cs','void Load(){LoadGarage();}','void Load(){LoadGarage();LoadInventory();}')
edit('Assets/Scripts/Game.cs','track=new GameObject(TrackWorld.Names[mapIndex]).AddComponent<TrackWorld>();track.Generate(mapIndex);RoadGuideLights.Build(track);track.ConnectFeatures(this);ApplyDeviceQuality();','RushReset();track=new GameObject(rushMode?Challenge.name:TrackWorld.Names[mapIndex]).AddComponent<TrackWorld>();if(rushMode)track.GenerateRush(rushIndex);else {track.Generate(mapIndex);RoadGuideLights.Build(track);}track.ConnectFeatures(this);ApplyDeviceQuality();')
edit('Assets/Scripts/Game.cs','for(int i=0;i<=aiCount;i++){int n=(TrackWorld.Count-2-i/2*2)%TrackWorld.Count;Vector3 p=track.points[n]+track.Right(n)*(i%2==0?-1:1)*(track.map==1?1.65f:3)+Vector3.up*.55f;','for(int i=0;i<=(rushMode?Challenge.opponents:aiCount);i++){int n=rushMode?0:(TrackWorld.Count-2-i/2*2)%TrackWorld.Count;Vector3 p=rushMode?new Vector3((i%2==0?-1:1)*3,5.55f,-12-i/2*7):track.points[n]+track.Right(n)*(i%2==0?-1:1)*(track.map==1?1.65f:3)+Vector3.up*.55f;')
edit('Assets/Scripts/Game.cs','if(powerups)foreach(Vector3 p in track.pickups)','if(powerups||rushMode)foreach(Vector3 p in track.pickups)')
edit('Assets/Scripts/Game.cs','state=State.Race;Toast("Go!");','state=State.Race;RushGo();Toast("Go!");')
edit('Assets/Scripts/Game.cs','if(Input.GetKeyDown(KeyCode.Escape))HandleBack();','RushUpdate();if(Input.GetKeyDown(KeyCode.Escape))HandleBack();')
edit('Assets/Scripts/Game.cs','public void OnFinish(Vehicle v){finishedCount++;','public void OnFinish(Vehicle v){if(rushMode){AwardRush(v);return;}finishedCount++;')
edit('Assets/Scripts/Game.cs','if(state==State.Menu)NewMenuGUI();else RaceGUI();','if(state==State.Menu)RushMenuGUI();else if(rushMode)RushRaceGUI();else RaceGUI();')
edit('Assets/Scripts/Game.cs','bool Button(string s,float x,float y,float w,float h=48,bool primary=false){GUI.backgroundColor=primary?new Color(.14f,.62f,.60f):new Color(.14f,.20f,.29f);bool b=GUI.Button(new Rect(x,y,w,h),s,button);GUI.backgroundColor=Color.white;return b;}','bool Button(string s,float x,float y,float w,float h=48,bool primary=false){return CustomButton(s,new Rect(x,y,w,h),primary);}')
edit('Assets/Scripts/Game.cs','void Panel(float x,float y,float w,float h,float a=.94f){GUI.color=new Color(.045f,.07f,.12f,a);GUI.DrawTexture(new Rect(x,y,w,h),pixel);GUI.color=Color.white;}','void Panel(float x,float y,float w,float h,float a=.94f){DrawPanel(new Rect(x,y,w,h),a);}')
edit('Assets/Scripts/Game.cs','v.item=WeaponCatalog.Items[UnityEngine.Random.Range(0,WeaponCatalog.Items.Length)];','v.item=game.rushMode?RushWeapons[UnityEngine.Random.Range(0,RushWeapons.Length)]:WeaponCatalog.Items[UnityEngine.Random.Range(0,WeaponCatalog.Items.Length)];')
edit('Assets/Scripts/Game.cs','public class Pickup:MonoBehaviour {','public class Pickup:MonoBehaviour {\n  public static readonly string[] RushWeapons={"Rocket","Rocket","Ice Blast","EMP","Oil Slick","Gravity Mine","Shockwave","Pulse","Boost","Shield"};')
edit('Assets/Scripts/Vehicle.cs','engineTune=tune*.4f;brakeTune=brakes*.6f;gripTune=tires*.13f;','engineTune=tune*.4f;brakeTune=brakes*.6f;gripTune=tires*.13f;if(isPlayer&&g.inventory!=null){if(g.inventory.engine=="sun-turbo")engineTune+=1.5f;if(g.inventory.chassis=="grip-kit")gripTune+=.6f;}')
edit('Assets/Scripts/Vehicle.cs','progress=lastPoint>TrackWorld.Count/2?lastPoint-TrackWorld.Count:lastPoint;','progress=track.isRush?0:lastPoint>TrackWorld.Count/2?lastPoint-TrackWorld.Count:lastPoint;')
edit('Assets/Scripts/Vehicle.cs','Time.deltaTime*27);','Time.deltaTime*27*(player&&game.inventory.engine=="storm-coil"?.8f:1));')
edit('Assets/Scripts/Vehicle.cs','if(delta>TrackWorld.Count/2)delta-=TrackWorld.Count;if(delta<-TrackWorld.Count/2)delta+=TrackWorld.Count;','if(!track.isRush){if(delta>TrackWorld.Count/2)delta-=TrackWorld.Count;if(delta<-TrackWorld.Count/2)delta+=TrackWorld.Count;}')
edit('Assets/Scripts/Vehicle.cs','if(!finished&&progress>=game.laps*TrackWorld.Count)','if(!finished&&(track.isRush?progress>=TrackWorld.Count-2&&transform.position.z>=track.RushEnd&&track.InRaceCorridor(transform.position):progress>=game.laps*TrackWorld.Count))')
edit('Assets/Scripts/Vehicle.cs','int aim=(near+ahead)%TrackWorld.Count;','int aim=track.isRush?Mathf.Min(near+ahead,TrackWorld.Count-1):(near+ahead)%TrackWorld.Count;')
edit('Assets/Scripts/Vehicle.cs','Vector3 target=track.points[aim]+track.Right(aim)*aiLane;','if(track.isRush){preferred=Mathf.Sin(carIndex*2.4f+driver.Length)*7;foreach(var hazard in track.rushHazards){var local=transform.InverseTransformPoint(hazard.transform.position);if(local.z>0&&local.z<100&&Mathf.Abs(hazard.transform.position.x-preferred)<4)preferred=hazard.transform.position.x>0?-6:6;}if(Physics.SphereCast(transform.position+Vector3.up,1.2f,transform.forward,out var obstacle,45,(1<<10)|1,QueryTriggerInteraction.Ignore)&&obstacle.normal.y<.5f)preferred=transform.position.x>0?-6:6;aiLane=Mathf.MoveTowards(aiLane,Mathf.Clamp(preferred,-8,8),Time.deltaTime*5);}Vector3 target=track.points[aim]+track.Right(aim)*aiLane;if(track.isRush&&near>TrackWorld.Count-8)target.z=track.RushEnd+120;')
edit('Assets/Scripts/Vehicle.cs','body.AddForce(impulse+Vector3.up*4,ForceMode.VelocityChange);','if(player&&game.inventory.chassis=="rock-plate")impulse*=.75f;body.AddForce(impulse+Vector3.up*4,ForceMode.VelocityChange);')
edit('Assets/Scripts/TrackWorld.cs','public Vector3 Forward(int i)=>(points[(i+1+Count)%Count]-points[(i-1+Count)%Count]).normalized;','public Vector3 Forward(int i)=>isRush?Vector3.forward:(points[(i+1+Count)%Count]-points[(i-1+Count)%Count]).normalized;')
edit('Assets/Scripts/TrackWorld.cs','public int Nearest(Vector3 p){int best=0;','public int Nearest(Vector3 p){if(isRush)return Mathf.Clamp(Mathf.RoundToInt(p.z/length*(Count-1)),0,Count-1);int best=0;')
edit('Assets/Scripts/TrackFeatures.cs','public bool Broken(int i)=>map==1?false:','public bool Broken(int i)=>isRush?false:map==1?false:')
edit('Assets/Scripts/TrackRecovery.cs','public bool InRaceCorridor(Vector3 position){','public bool InRaceCorridor(Vector3 position){\n   if(isRush)return Mathf.Abs(position.x)<=12.65f&&position.z>=-110&&position.z<=length+200&&position.y>2;')
edit('Assets/Scripts/TrackRecovery.cs','int n=(near+offset+Count)%Count;bool ramp=false;','int n=isRush?Mathf.Clamp(near+offset,0,Count-1):(near+offset+Count)%Count;if(isRush&&n>Mathf.Max(0,car.lastPoint))continue;bool ramp=false;')
edit('Assets/Editor/BuildGame.cs','var shaders=new[]{Shader.Find("Aether/Ice")','var shaders=new[]{Shader.Find("Aether/RushSky"),Shader.Find("Aether/Ice")')
edit('Assets/Scripts/PlatformUI.cs','Panel(0,0,1600,900,1);if(logo)','DrawRushBackdrop(0);Panel(355,20,890,860,.78f);if(logo)')
